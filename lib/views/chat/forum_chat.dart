import 'package:flutter/material.dart';

class ForumChat extends StatelessWidget {
  const ForumChat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Forum Section",
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
